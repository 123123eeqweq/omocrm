{
  "name": "backend",
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/index.js",
    "dev:win": "cross-env NODE_OPTIONS=--openssl-legacy-provider node --watch src/index.js",
    "start": "node src/index.js",
    "start:win": "cross-env NODE_OPTIONS=--openssl-legacy-provider node src/index.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.21.0",
    "express-session": "^1.19.0",
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
