# omocrm

Full-stack проект с React frontend и Node.js backend.

## Структура

- `frontend/` — React + Vite + TypeScript
- `backend/` — Node.js + Express + PostgreSQL

## Запуск

См. README в папках `frontend/` и `backend/`.
